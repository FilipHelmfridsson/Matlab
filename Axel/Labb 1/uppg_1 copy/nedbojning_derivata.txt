function [duzx] = nedbojning_derivata(x)
    M0 = 1;
    L = 3;
    E = 1;
    I = 1;

    R = M0 / (E * I);

    duzx = (R * (L^2 / 12)) * ( ...
        (32 / L) ...
        - (54 * x / L^2) ...
        - (15 * x.^2 / L^3) ...
        - (9 * pi / L) * cos((pi * x) / L) ...
    );
end
